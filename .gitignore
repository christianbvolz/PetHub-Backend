# --- SECURITY (CRITICAL) ---
# Ignore the file containing TiDB password
.env

# --- .NET BUILD FILES ---
# We don't want to upload the generated executable, only the source code
[Bb]in/
[Oo]bj/

# --- USER/IDE SETTINGS ---
# VS Code local settings (optional, but recommended to ignore)
.vscode/
# Visual Studio cache (if opened on Windows later)
.vs/
*.user

# --- SYSTEM FILES ---
.DS_Store
Thumbs.db

# --- LOGS AND TESTS ---
*.log
TestResults/

# --- APPSETTINGS ---
# appsettings.json and Development.json usually GO UP to git.
# But if you create a specific file for your local machine, ignore it:
appsettings.Local.json